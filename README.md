scraping
========
This is some scrapy code to scrape backcountry.com. Careful - right now it's set up to scrape all men's product which is > 13K products.

To run the scraper, type the following in the command line:
scrapy crawl back -o backcountry.json -t json
